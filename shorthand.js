//  % is called modulus or remainder
//  ** is called exponentiation
// / is called division

//  x + y = z ; here x and y are called operands , + and = are called operators . z is the result.

var price = 10;
//  price = price + 10;
// price += 10;
// price -= 10;
price *= 10;
 console.log(price);